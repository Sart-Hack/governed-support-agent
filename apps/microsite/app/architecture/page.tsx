import { PageShell } from "../components/page-shell";

export default function ArchitecturePage() {
  return (
    <PageShell
      eyebrow="Reference"
      title="Architecture"
      intro="How a ticket becomes a governed action: the agent, the agent-shield policy decision point, the MCP servers it reaches through, and the trace tree every step writes to Langfuse."
    />
  );
}
