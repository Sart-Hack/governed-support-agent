import { PageShell } from "../components/page-shell";

export default function RefusalsPage() {
  return (
    <PageShell
      eyebrow="Demo"
      title="Refusals"
      intro="What the agent will not do, in motion. Each scene replays a recorded run where a Cedar policy denies the action and renders the reason chain: delete account, indirect injection, PII leak, kill switch."
    />
  );
}
