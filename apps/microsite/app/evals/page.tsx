import { PageShell } from "../components/page-shell";

export default function EvalsPage() {
  return (
    <PageShell
      eyebrow="Governance"
      title="Evals"
      intro="Three suites keep the policies honest: an InjecAgent subset, a scenario-derived custom suite, and one assertion per OWASP Agentic Top 10 ID. Pass rates track over the last 30 CI runs."
    />
  );
}
